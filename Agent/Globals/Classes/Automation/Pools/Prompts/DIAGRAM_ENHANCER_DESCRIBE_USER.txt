Consider the uploaded image and generate an exhaustive description of the figure such that it can be fed to an image generation model to recreate the exact same thing.

Extract all the facts from the image exhaustively and ensure that all factual details, labels and where they are supposed to be, without fail and accurately. If there are values in the image can be changed, change it. Do not capture the caption or any irrelevant text -- figure numbers, source attribution lines, page numbers, running headers or footers, watermarks, course codes, or any other surrounding page text that is not part of the diagram itself. Also ensure you don't enforce any shapes or even colors unless absolutely needed. You just need to get the entire concept. If there are connections, relationships or links, make sure you capture all of them with directionality, ends and/or any attributes but not the shape/color or layout. Shapes and colors are ONLY described when it is needed for the diagram to make sense.

Write the description as plain, flowing English prose in complete sentences. Do NOT format it as JSON, as key/value pairs, as a code block, or wrapped in backticks -- it must read as a paragraph a person could follow.