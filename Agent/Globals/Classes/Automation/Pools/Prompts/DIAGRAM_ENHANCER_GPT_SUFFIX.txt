Create a NEW figure from the description above. It must convey every fact, label, value and relationship the description states -- omit nothing -- while being an original drawing rather than a reproduction of any existing figure. Choose your own layout, arrangement, shapes and colours, except where the description explicitly says a shape or colour carries meaning; follow those exactly.

Draw ONLY the figure. The output is a standalone diagram, not a page from a book: no body text, no paragraphs, no caption, no figure number, no title bar, no page number, no header or footer, and no explanatory sentences anywhere in the image. The only text permitted is the labels, axis titles, units, legend entries and annotations that belong inside the figure itself.

Every text label must be fully readable and must NOT overlap or touch any shape, line, arrow, box, or other text -- place each label cleanly inside or beside the element it belongs to, with clear empty space around it, and enlarge the layout rather than letting anything crowd. Ensure there are no overlaps or untidy bits anywhere in the image and make sure all the aspects in the description are covered.
