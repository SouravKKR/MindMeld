Look at these {batch_size} images. For each one, decide whether it should appear in a study deck. Include the image if it contains technical detail, illustrates something that could be discussed in the surrounding text, or looks like the kind of figure that could be asked about in an exam. Exclude headers, footers, page numbers, watermarks, logos, decorative borders, and any other ambient junk. Reply strictly with a JSON array containing exactly {batch_size} objects. Each object must have imageCategory (string), isEducationalContent (boolean), and visionModelGeneratedDescription (string). If false, leave the description empty. If true, write a dense 2-sentence description of the visual concept.