Look at these {batch_size} images. For each one, decide whether it should appear in a study deck.

Use one simple test: does the image itself carry information a student could study or be tested on? Keep it only when it does — for example a diagram, chart, graph, plot, table, map, timeline, labelled illustration, schematic, anatomical or scientific drawing, formula, or a worked example. These show real structure, data, parts, steps, or relationships.

Drop everything that is only decorative or generic and teaches nothing on its own — photographs or cartoon illustrations of people or scenes, mascots, motivational or mood-setting pictures, clip-art, stock imagery, icons, logos, headers, footers, page numbers, watermarks, and borders. If the image merely decorates the page or sets a mood — even when it loosely fits the topic — drop it. When you are unsure, drop it: an irrelevant image in a study deck is worse than a missing one.

Reply strictly with a JSON array containing exactly {batch_size} objects. Each object must have imageCategory (string), isEducationalContent (boolean), and visionModelGeneratedDescription (string). If false, leave the description empty. If true, write a dense 2-sentence description of the visual concept.