Below is an indented outline extracted from a textbook on {subject_name}. Indentation represents hierarchy depth — the least-indented lines are the book's top-level chapters/units, and more-indented lines are their sections and sub-sections.

Some entries may be noise picked up during extraction — raw data, table contents, figure labels, or other non-topic text. Discard anything that would not make sense as a standalone topic in {subject_name}. Only keep entries that a student would recognise as something worth studying.

Convert the clean result into a nested JSON curriculum tree where leaf nodes are arrays of specific concept strings. Keep the outline's top-level divisions (its chapters/units) as the top level of your JSON — one root key per source chapter, in the original order. Do NOT flatten, merge, or fold the chapters under a single overarching heading, and do NOT use "{subject_name}" itself as a chapter.

-- OUTLINE BEGIN --
